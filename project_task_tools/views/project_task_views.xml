<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <record id="view_task_form_tools" model="ir.ui.view">
    <field name="name">project.task.form.tools</field>
    <field name="model">project.task</field>
    <field name="inherit_id" ref="project.view_task_form2"/>
    <field name="arch" type="xml">
      <xpath expr="//sheet/notebook" position="inside">
        <page string="Herramientas" name="tools_tab"
              invisible="not use_task_tools">
          <field name="use_task_tools" invisible="1"/>
          <field name="done_task_tools" invisible="1"/>
          <field name="tool_line_ids" readonly="done_task_tools">
            <list editable="bottom">
              <field name="task_done_task_tools" invisible="1"/>
              <field name="product_id"/>
              <field name="qty"/>
              <field name="uom_id" readonly="1"/>
              <field name="date_from"/>
              <field name="date_to"/>
              <field name="picking_type_id"/>
              <field name="state" readonly="1"/>
              <field name="picking_out_id" readonly="1"/>
              <field name="picking_in_id" readonly="1"/>
              <button name="action_entregar" string="Entregar" type="object"
                      invisible="state != 'draft' or task_done_task_tools"/>
              <button name="action_devolver" string="Devolver" type="object"
                      invisible="state != 'out' or task_done_task_tools"/>
            </list>
          </field>
        </page>
      </xpath>
    </field>
  </record>
</odoo>
