<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <!-- Acción de ventana -->
  <record id="action_account_group" model="ir.actions.act_window">
    <field name="name">Grupos de cuentas</field>
    <field name="res_model">account.group</field>
    <field name="view_mode">list,form</field>
    <field name="help" type="html">
      <p>Organiza el plan contable por prefijos de código y jerarquías.</p>
    </field>
  </record>

  <!-- Menú bajo Contabilidad ▸ Configuración -->
  <menuitem id="menu_account_group"
            name="Grupos de cuentas"
            parent="account.menu_finance_configuration"
            action="action_account_group"
            groups="account.group_account_user,account.group_account_manager"
            sequence="9"/>
</odoo>
